<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_expenses_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="org.phomellolitepos.ExpensesListActivity"
    tools:showIn="@layout/activity_expenses_list">

    <TextView
        android:id="@+id/expense_title"
        android:text="@string/No_Expnse_Fnd"
        style="@style/AppTheme.NoListfoundText"/>

    <ListView
        android:id="@+id/expense_list"
        android:layout_below="@+id/expense_title"
        style="@style/AppTheme.ListView">

    </ListView>

</RelativeLayout>
